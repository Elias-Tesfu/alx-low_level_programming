#include "main.h"

/**
 * is_palindrome - returns 1 if string is palindrome and 0 if not
 * @s: the string
 * Return: 1 if no error and 0 if there is error
 */

int is_palindrome(char *s)
{
	char = c;
	s++;
	if (*s > '\0')
	{
		c = s;
	}
	i
