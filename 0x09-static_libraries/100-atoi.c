#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _atoi - converts a string to an integer
 * @s: the string
 * Return: the inteege
 */

int _atoi(char *s)
{
	int n;

	n = atoi(s);
	return (n);
}
